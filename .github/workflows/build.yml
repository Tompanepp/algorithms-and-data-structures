name: C/C++ CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: build-array
      run: |
        cd ./src
        make -C 2.array/src/ lib_array.a
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - uses: actions/checkout@v2
    - name: test-array
      run: |
        cd ./src
        make -C 2.array/test/ test-program
