/*
    1. construct
    2. ...

    Represetation:
        struct Array
            pointer to starting index
*/
#include "type.h"

#ifndef ARRAY_H
#define ARRAY_H

struct array;
struct array construct(type dataType, int size);

#endif
